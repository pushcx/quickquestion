#!/usr/bin/env ruby

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)
require "bundler/setup"

require "readline"
require "qq"

input = ARGV.any? ? ARGV.join(' ') : ($stdin.tty? ? Readline.readline("> ", true) : $stdin.read)
response = QQ.query(input)
puts response
